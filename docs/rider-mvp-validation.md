# Rider MVP 联调与回归记录

## 本轮验证范围

- 骑手端登录与路由守卫（`/rider/login`、`/rider`）
- 骑手状态切换（在线/离线）
- 订单履约链路（接单 -> 到店 -> 取餐 -> 送达）
- 用户端/商家端主流程未回归破坏（静态检查）

## 已完成验证

- 前端构建：`cd frontend && npm run build` 通过。
- 接口合同文档：`docs/backend-api.md` 已补全 Rider MVP 章节。
- 数据库脚本：`docs/init.sql` 与 `docs/patch-rider.sql` 已覆盖骑手表和订单扩展字段。

## 未完成验证与原因

- 后端编译与接口联调未在当前环境完成：本机缺少 Java Runtime（`Unable to locate a Java Runtime`）。

## 下一迭代缺陷与待办

- P1：补齐 Java 运行时后执行后端编译与集成测试（重点覆盖并发接单场景）。
- P1：执行一次端到端联调脚本（用户下单支付 -> 骑手接单履约 -> 用户订单状态确认）。
- P2：完善骑手送达后的订单归档策略（当前复用 `status=4` 为已完成）。
- P2：增加骑手位置上报接口实现（文档已定义，代码暂未接入）。
